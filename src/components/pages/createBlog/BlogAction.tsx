const BlogAction = () => {
  // const { title, description, tags } = useAppSelector(
  //   (state: RootState) => state.BlogForm
  // );

  // const handleClick = (e) => {
  //   e.preventDefault();
  //   console.log({ title, description, tags });
  // };

  return (
    <>
      {/* <ActionButton onClick={() => console.log({ title, description, tags })}> */}
      {/* Publish */}
      {/* </ActionButton> */}
      <button type="submit">publish</button>
    </>
  );
};

export default BlogAction;
